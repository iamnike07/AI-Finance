# Postgres connection (matches docker-compose.yml defaults)
DATABASE_URL=postgresql://investor:investor_dev_password@localhost:5432/investor_platform

# LLM API key for extraction + chat (Google Gemini free tier — get one at ai.google.dev)
GEMINI_API_KEY=your_key_here
